const multiplicar = (number1: number, number2: number): number => number1 * number2

const saudação = (nome: string): string => `Olá, ${nome}`

console.log(saudação("David"))

console.log(multiplicar(10, 2))